# DFX ClawOps - Claude Code Memory

Read AGENTS.md first. It is the canonical, cross-agent ruleset (SPEC-AGENT-INSTRUCTIONS-DEDUP-01, 2026-07-04) — all shared rules, verification gates, and test policy live there now. This file holds only the Claude-Code-specific delta.

## Claude Code identity delta

`commit_message.format` の Co-Authored-By 行 (AGENTS.md 参照) では、Claude Code は以下の識別子を使用する:

```
Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
